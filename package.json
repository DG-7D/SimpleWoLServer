{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run build && npm run start",
    "start": "node ./dist/server.js",
    "build": "npx tsc && node ./src/build.ts",
    "copyAssets": "node -e \"fs.cpSync('./public/', './dist/', { recursive: true })\""
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.2",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}